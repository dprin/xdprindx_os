Extract:
	 tar xvf os.tar.gz -C /home/jordan/Desktop/Programming/TeamOS/

In the Operating System folder ("Named OS"), open the Terminal and type:
	make clean
*new line*	
	make

Go into the directory "testing" in Terminal using the following command:
	cd testing

Then type to run the operating system this command:
	./os


gg.